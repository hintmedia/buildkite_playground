steps:
  - label: ":rspec:"
    commands:
      - "bundle"
      - "rails db:reset"
      - "bundle exec rake knapsack:rspec"
    parallelism: 5
    plugins:
      - docker#v2.0.0:
        image: "ruby:2.7.2"
        workdir: /app
        env:
          - KNAPSACK_GENERATE_REPORT=true
